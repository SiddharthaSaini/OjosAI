services:
  - type: web
    name: eyes-disease-detection
    env: python
    pythonVersion: 3.10.13
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
